import { Workspace } from "@/components/workspace";

/**
 * Static design demo — the original mockup port with hardcoded fixture data.
 * Kept as a reference for the Phase 3 workspace build-out.
 */
export default function DemoPage() {
  return <Workspace />;
}
